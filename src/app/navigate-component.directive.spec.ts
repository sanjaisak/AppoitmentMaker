import { NavigateComponent } from './navigate-component.directive';

describe('HostDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigateComponent();
    expect(directive).toBeTruthy();
  });
});
